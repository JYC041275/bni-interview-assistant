export const CONFIG = {
    // 預設 API Key (可選)
    // 如果此處留空，使用者需要在 UI 中輸入 API Key
    API_KEY: '',

    // 是否在 UI 顯示 API Key 輸入框
    // 如果您希望使用者輸入自己的 Key，請設為 true
    // 如果您已在上方設定了 API_KEY 且不想讓使用者更改，可設為 false
    SHOW_API_KEY_INPUT: true,
};
